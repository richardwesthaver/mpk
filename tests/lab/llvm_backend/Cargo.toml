[package]
name = "llvm_backend"
version = "0.1.0"
edition = "2021"

[dependencies]
inkwell = { git = "https://github.com/TheDan64/inkwell", branch = "master", features = ["llvm13-0"] }
mpk = { path = "../../../lib", features = ["parser"] }